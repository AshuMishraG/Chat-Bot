name: ChatBot CI Deploy

on:
    push:
        branches: [ develop ]



jobs:
    # Lint:
    #     name: Lint
    #     runs-on: ubuntu-latest

    #     permissions:
    #       contents: read
    #       packages: read
    #       statuses: write

    #     steps:
    #       - name: Checkout code
    #         uses: actions/checkout@v4
    #         with:
    #           fetch-depth: 0

    #       - name: Super-linter
    #         uses: super-linter/super-linter@v7.4.0
    #         env:
    #           GITHUB_TOKEN: ${{ secrets.SUPER_LINTER_GITHUB_TOKEN }}
    #           VALIDATE_PYTHON_PYLINT: true
    #           ENABLE_GITHUB_ACTIONS_STEP_SUMMARY: true
    #           SAVE_SUPER_LINTER_OUTPUT: true
    deploy:
        name: Setup Gcloud Account
        # needs: Lint
        runs-on: ubuntu-latest
        env:
            IMAGE_NAME: us-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/chatbot/${{ vars.GCP_CI_APP_NAME }}

        steps:
          - name: Checkout repository
            uses: actions/checkout@v4

          - name: Google Auth
            id: auth
            uses: google-github-actions/auth@v2
            with:
              credentials_json: ${{ secrets.GCP_CREDENTIALS }}

          - name: Docker Auth
            id: docker-auth
            uses: 'docker/login-action@v3'
            with:
              registry: us-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}
              username: _json_key
              password: ${{ secrets.GCP_CREDENTIALS }}

          - name: Build Docker image
            run: docker build . -t $IMAGE_NAME

          - name: Push Docker image
            run: docker push $IMAGE_NAME

          - name: Deploy to Cloud Run
            id: deploy
            uses: google-github-actions/deploy-cloudrun@v2
            with:
              service: ${{ vars.GCP_CI_APP_NAME }}
              region: us-central1
              flags: '--allow-unauthenticated --port 8000'
              image: ${{ env.IMAGE_NAME }}

          - name: Show Output
            run: echo ${{ steps.deploy.outputs.url }}